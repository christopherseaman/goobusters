ipython
matplotlib
# mdai 0.16.0 with compatibility fix for current pydicom versions
mdai>=0.16.0
numpy>=1.21.0,<3.0.0  # Support both NumPy 1.x and 2.x
# IMPORTANT: Only install opencv-contrib-python, NOT opencv-python
# Having both causes conflicts and breaks optical flow methods
opencv-contrib-python  # Latest version with all contrib modules
pandas
pillow
# Use current pydicom with compatibility bridge in lib/optical.py
pydicom>=3.0.0
python-dotenv
scikit-image
scipy
torch>=2.0.0  # Use newer PyTorch that supports NumPy 2.x
torchvision>=0.15.0  # Compatible with newer PyTorch
tqdm
pyyaml
psutil  # For performance monitoring
flask  # For annotation review web app
httpx  # For test scripts (HTTP client)
imageio[pyav]  # Video frame extraction (shared server/iOS)
PyJWT  # JWT for App Store Connect API
cryptography  # Required by PyJWT for ES256