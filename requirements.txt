ipython
matplotlib
# mdai 0.16.0 with compatibility fix for current pydicom versions
mdai==0.16.0
numpy
# IMPORTANT: Only install opencv-contrib-python, NOT opencv-python
# Having both causes conflicts and breaks optical flow methods
opencv-contrib-python  # Latest version with all contrib modules
pandas
pillow
# Use current pydicom with compatibility bridge in lib/optical.py
pydicom>=3.0.0
python-dotenv
scikit-image
scipy
torch
torchvision
tqdm